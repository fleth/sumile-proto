syntax = "proto3";

option java_package = "bottlefoxy.sumile";

service Strategy {
    rpc strategy (StrategyRequest) returns (StrategySetting) {}
}

message StrategyRequest {
    Env env = 1;

    enum Env {
        PRODUCTION = 0;
        DEVELOP = 1;
    }
}

message StrategySetting {
    string key = 1;
    repeated string and = 2;
    repeated string any = 3;
}
